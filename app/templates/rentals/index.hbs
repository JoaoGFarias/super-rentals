{{#list-filter filter=(action 'filterByCity') as |filteredResults|}}
<ul class="results">
  {{#if filteredResults}} {{#each filteredResults as |rentalUnit|}}
  <li>{{rental-listing rental=rentalUnit}}</li>
  {{/each}} {{else}} Sorry. No rentals found. {{/if}}
</ul>
{{/list-filter}}